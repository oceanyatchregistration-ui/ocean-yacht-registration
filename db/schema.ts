import {sqliteTable,text,integer,real,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const users=sqliteTable('users',{id:text('id').primaryKey(),identityId:text('identity_id').notNull().unique(),email:text('email').notNull(),role:text('role').notNull().default('ADMIN'),createdAt:text('created_at').notNull()});
export const customers=sqliteTable('customers',{id:text('id').primaryKey(),type:text('type').notNull().default('INDIVIDUAL'),name:text('name').notNull(),email:text('email').notNull(),phone:text('phone').notNull(),address:text('address').notNull(),city:text('city').notNull(),postalCode:text('postal_code').notNull(),country:text('country').notNull(),createdAt:text('created_at').notNull()});
export const vessels=sqliteTable('vessels',{id:text('id').primaryKey(),name:text('name').notNull(),type:text('type').notNull(),builder:text('builder'),model:text('model'),buildYear:integer('build_year'),identification:text('identification'),length:real('length').notNull(),currentFlag:text('current_flag'),intendedUse:text('intended_use').notNull()});
export const services=sqliteTable('services',{id:text('id').primaryKey(),name:text('name').notNull(),active:integer('active').notNull().default(1)});
export const serviceOptions=sqliteTable('service_options',{id:text('id').primaryKey(),serviceId:text('service_id').notNull().references(()=>services.id),name:text('name').notNull(),active:integer('active').notNull().default(1)});
export const pricingRules=sqliteTable('pricing_rules',{id:text('id').primaryKey(),serviceId:text('service_id').notNull().unique().references(()=>services.id),amountMinor:integer('amount_minor').notNull(),currency:text('currency').notNull(),active:integer('active').notNull().default(1),updatedAt:text('updated_at').notNull()});
export const applications=sqliteTable('applications',{id:text('id').primaryKey(),reference:text('reference').notNull().unique(),customerId:text('customer_id').references(()=>customers.id),vesselId:text('vessel_id').references(()=>vessels.id),serviceId:text('service_id').references(()=>services.id),status:text('status').notNull().default('DRAFT'),draftPayload:text('draft_payload').notNull().default('{}'),pricingSnapshot:text('pricing_snapshot'),consentAt:text('consent_at'),consentVersion:text('consent_version'),submittedAt:text('submitted_at'),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull(),version:integer('version').notNull().default(0),lastActorId:text('last_actor_id').references(()=>users.id),lastPublicMessage:text('last_public_message')},t=>[index('idx_app_status_submitted').on(t.status,t.submittedAt)]);
export const sessions=sqliteTable('draft_sessions',{tokenHash:text('token_hash').primaryKey(),applicationId:text('application_id').notNull().references(()=>applications.id),expiresAt:text('expires_at').notNull()});
export const documents=sqliteTable('application_documents',{id:text('id').primaryKey(),applicationId:text('application_id').notNull().references(()=>applications.id),publicId:text('public_id').notNull().unique(),type:text('type').notNull(),filename:text('original_filename').notNull(),storageKey:text('storage_key').notNull().unique(),mimeType:text('mime_type').notNull(),size:integer('file_size').notNull(),sha256:text('sha256').notNull(),uploadedAt:text('uploaded_at').notNull()},t=>[index('idx_documents_application').on(t.applicationId)]);
export const history=sqliteTable('application_status_history',{id:text('id').primaryKey(),applicationId:text('application_id').notNull().references(()=>applications.id),fromStatus:text('from_status'),toStatus:text('to_status').notNull(),actorId:text('actor_id').references(()=>users.id),publicMessage:text('public_message'),createdAt:text('created_at').notNull(),version:integer('version').notNull()},t=>[uniqueIndex('idx_history_application_version').on(t.applicationId,t.version)]);
export const notifications=sqliteTable('notifications',{id:text('id').primaryKey(),applicationId:text('application_id').notNull().references(()=>applications.id),kind:text('kind').notNull(),channel:text('channel').notNull().default('ADMIN_INBOX'),createdAt:text('created_at').notNull(),readAt:text('read_at')},t=>[index('idx_notifications_unread').on(t.readAt)]);
export const rateLimits=sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expiresAt:integer('expires_at').notNull()});
